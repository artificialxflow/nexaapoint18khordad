# NexaApp — TODO v2 (فرانت‌محور، بدون بک‌اند)

قرارداد: تا اعلام خلاف، همهٔ موارد زیر **فقط UI + state محلی (+ mock)** است؛ API/DB خارج از اسکوپ فعلی.

پیاده‌سازی اولیهٔ استور مرکزی و ویوهای اشخاص/کالا/فروش در کد انجام شده؛ موارد زیر برای تکمیل تدریجی، سخت‌گیری QA و اتصال بک‌اند نگه داشته می‌شوند.

## 0) اصول معماری فرانت

- [x] تعریف **نوع دادهٔ مرکزی** برای `Person` (و زیرساختارها) در TypeScript — [`src/types/person.ts`](src/types/person.ts)
- [x] تعریف **نوع دادهٔ مرکزی** برای `Product` و **قیمت‌های چندگانه** و انبار/بارکد — [`src/types/product.ts`](src/types/product.ts)
- [x] انتخاب الگوی state: **React Context** در [`src/context/CatalogContext.tsx`](src/context/CatalogContext.tsx) (پوشش در [`src/layouts/DashboardShell.tsx`](src/layouts/DashboardShell.tsx))
- [x] مستندسازی کوتاه: نمونهٔ شکل داده در پایین همین فایل

## 1) هم‌ترازی واژگان (کارفرما)

- [x] در ماژول اشخاص: **«زیرمجموعه»** به‌جای «فروشنده» برای نقش سازمانی
- [x] اصلاح متن توضیحی هدر People
- [x] تفکیک معنایی در فروش: **«سهم فروش (قرارداد)»** در برابر «زیرمجموعهٔ سازمانی» — [`src/views/Sales.tsx`](src/views/Sales.tsx)
- [x] تفکیک برچسب در مالی — [`src/views/Finance.tsx`](src/views/Finance.tsx)
- [ ] بازبینی نهایی تمام رشته‌های فارسی پروژه برای واژگان یکسان

## 2) ماژول اشخاص — ساختار صفحه

- [x] مسیر master-detail + **تب‌های** عمومی / اطلاعات / تماس / دسته‌بندی و گروه‌ها
- [x] تب عمومی: حقیقی/حقوقی؛ نام؛ **نام مستعار**؛ چک‌باکس نقش‌ها (مشتری، تأمین‌کننده، سهامدار، زیرمجموعه)
- [x] انتخاب **لیست قیمت پیش‌فرض** از mock
- [x] **پیشنهاد هوشمند** هنگام تایپ نام/مستعار
- [x] تب اطلاعات: چند **آدرس**، تیک **آدرس اصلی**، lat/lng
- [x] **نقشه (placeholder)** + دکمهٔ انتخاب تقریبی موقعیت
- [x] چند **تلفن** با برچسب
- [x] چند **حساب بانکی**
- [x] **تاریخ‌های مهم** به‌صورت لیست
- [x] دسته‌بندی و **گروه + سطح** (دادهٔ mock قابل ویرایش در UI)
- [ ] وایرفریم نهایی کارفرما برای ترتیب تب‌ها و نام‌گذاری

## 3) ماژول اشخاص — رفتار یکتایی (ماک)

- [x] تشخیص تکراری **موبایل** و **کد ملی** در برابر دادهٔ موجود
- [x] جریان **باز کردن پروفایل موجود** + بنر توضیحی
- [x] نرمال‌سازی سادهٔ شماره برای مچ

## 4) کارت حساب شخص

- [x] ناوبری به **کارت حساب** از لیست و از جزئیات
- [x] تب‌ها: **مالی**، **مارکتینگ**، **فعالیت**، **وضعیت سفارشات** با نمونهٔ دمو
- [ ] اتصال واقعی به اسناد مالی (فاز بک‌اند)

## 5) لیست اشخاص و فیلترها

- [x] تب‌های نما: همه، مشتریان، تأمین‌کنندگان، سهامداران، زیرمجموعه
- [x] جستجو (نام، موبایل، مستعار، کد ملی)
- [ ] فیلتر پیشرفتهٔ کامل (چندمعیاره)

## 6) ماژول کالاها و خدمات — عمق فرم

- [x] فیلدهای بارکد، واحد، انبار پیش‌فرض، توضیحات در مدل و فرم افزودن
- [x] **قیمت‌گذاری چندگانه** (خرده / عمده / همکار) در استور
- [x] انتخاب **بازهٔ قیمت نمایشی** در لیست/گرید
- [x] اتصال mock **لیست قیمت** به شخص و [`getPriceForPerson`](src/lib/pricing.ts)
- [x] دمو فاکتور: نمایش قیمت پیشنهادی بر اساس مشتری انتخاب‌شده
- [ ] ویرایش کالا در همان فرم عمیق (الان افزودن + لیست از استور)

## 7) یکپارچگی بین ماژول‌ها (هنوز mock)

- [x] انتخاب مشتری در Sales از استور اشخاص + نمایش نقش/سطح
- [x] [`getPriceForPerson`](src/lib/pricing.ts) برای دمو خط فاکتور
- [ ] همگام‌سازی کامل فاکتور با سطرهای کالا از استور

## 8) کیفیت و تست دستی

- [ ] چک RTL و موبایل برای فرم‌های چندتبی
- [ ] حالت خالی و seed بزرگ‌تر برای استرس UI
- [ ] `npm run build` و رفع warningهای مهم

## 9) فاز بعدی (بک‌اند)

- [ ] API، migration، unique constraint DB، auth
- [ ] نقشه با API key واقعی
- [ ] اتصال کارت حساب به اسناد مالی
- [ ] وب‌سوکت در صورت نیاز

---

## نمونهٔ شکل داده (راهنمای بک‌اند آینده)

```json
{
  "person": {
    "id": "uuid",
    "kind": "natural | legal",
    "displayName": "string",
    "legalName": "string?",
    "alias": "string",
    "nationalId": "string?",
    "roles": ["customer", "supplier", "shareholder", "subordinate"],
    "defaultPriceListId": "uuid?",
    "parentPersonId": "uuid?",
    "categories": ["categoryId"],
    "groupMemberships": [{ "groupId": "uuid", "levelId": "uuid" }],
    "addresses": [{ "id": "uuid", "text": "string", "isPrimary": true, "lat": 0, "lng": 0 }],
    "phones": [{ "id": "uuid", "label": "موبایل", "number": "09..." }],
    "banks": [{ "id": "uuid", "title": "string", "iban": "string" }],
    "importantDates": [{ "id": "uuid", "title": "تولد", "date": "۱۴۰۳/۰۱/۰۱" }],
    "status": "active | inactive",
    "points": 0
  },
  "product": {
    "id": "uuid",
    "name": "string",
    "code": "string",
    "type": "goods | services",
    "category": "string",
    "stock": "string",
    "barcode": "string?",
    "unit": "عدد?",
    "defaultWarehouse": "string?",
    "description": "string?",
    "prices": { "retail": 0, "wholesale": 0, "partner": 0 }
  },
  "priceList": { "id": "uuid", "name": "عمده", "tier": "wholesale | retail | partner" }
}
```
